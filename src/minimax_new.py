from email.errors import MultipartConversionError


class Minimax:    
    def minimax(self, board, snake_id, depth, print_output= False):
        '''       
        if depth = 0:
            return state
        
        snake = board.snakes[snake_id]
        potential__moves = {}
        
        Dictionaries aren't ordered I don't think, turn order list with ids?
        
        
        
        
        '''